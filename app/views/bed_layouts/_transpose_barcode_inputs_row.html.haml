-# This partial attaches the barcode_column_N css class to the rows of the
-# transpose layout to reuse the javascript css highlight, where N is the
-# index passed by the calling scope for the source and destination pair
-# specified in bed_ids.
- bed_ids.each do |bed_id_upcase|
  - bed_id = bed_id_upcase.downcase
  %fieldset{class: "barcode_column_#{index} barcode_column"}
    %legend= "#{bed_id_upcase}"
    %br/
    - ['bed','plate'].each do |row_id|
      %li
        %label.hidden{for: "#{bed_id}_#{row_id}"}
          = "#{row_id.titlecase} #{bed_id_upcase}"
        %label.layout_input_text= "#{row_id.titlecase}"
        = text_field_tag "#{bed_id}_#{row_id}", nil, :name => "robot[#{bed_id}][#{row_id}]", :size => 13
